[DEFAULT]
lr = 0.01
epochs = 100
; in_dim has to be exactly 300 in case we wish to use pre-initialized embeddings, since we are not using PCA on these for now...
in_dim = 300
hidden_dim = 300
train_split = 0.85
val_split = 0.9
